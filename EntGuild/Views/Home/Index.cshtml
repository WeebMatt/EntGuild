@model EntGuild.Models.ProductGenreViewModel

<style>

    .card-container {
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
    }

    .card {
        width: 200px;
        margin: 20px;
        min-height: 200px;
        padding: 10px;
        color: black;
    }

    a   {
        text-decoration: none;
    }

    .card:hover {
        transform: scale(1.2);
        transition: all 0.1s;
    }

    .card-title {
        font-weight: bold;
    }

</style>

@{
    ViewData["Title"] = "Home Page";
}

<h1>Entertainment Hub</h1>

<p>Welcome to Entertainment Hub where all your favourite Books, Movies and Games are located!</p>

<h2>Books Genre</h2>
<div class="card-container">
    
    @foreach (var product in Model.BooksProducts!)
    {
        <a asp-action="Details" asp-controller="Products" asp-route-id="@product.Id">
        <div class="card">
            <h3>@product.Name</h3>
            <hr/>
            <p>@product.Author</p>
        </div>
        </a>
    }
</div>

<h2>Movies Genre</h2>
<div class="card-container">
    @foreach (var product in Model.MoviesProducts!)
    {
        <a asp-action="Details" asp-controller="Products" asp-route-id="@product.Id">
        <div class="card">
            <h3>@product.Name</h3>
            <hr />
            <p>@product.Author</p>
        </div>
        </a>
    }
</div>

<h2>Games Genre</h2>
<div class="card-container">
    @foreach (var product in Model.GamesProducts!)
    {
        <a asp-action="Details" asp-controller="Products" asp-route-id="@product.Id">
        <div class="card">
            <h3>@product.Name</h3>
            <hr />
            <p>@product.Author</p>
        </div>
        </a>
    }
</div>